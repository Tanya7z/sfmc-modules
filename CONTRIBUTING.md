# Contributing

1. 用 [sfmc-module-template](https://github.com/Tanya7z/sfmc-module-template) 建独立仓
2. `npm publish`（社区 `@<user>/sfmc-module-<id>`；官方 `@sfmc-bds/module-<id>`）
3. PR 更新本仓 `index.json` 的 `npm` 条目，或走 `sfmc mod publish --gh-push`
