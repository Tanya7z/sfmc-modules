{
  "name": "sfmc-modules",
  "private": true,
  "description": "SFMC module discovery index (index.json only). Not a development monorepo.",
  "scripts": {
    "check": "node -e \"const j=require('./index.json'); if(j.version<2) process.exit(1); console.log('index ok', Object.keys(j.modules||{}).length, 'modules')\""
  }
}
